PRODUCTION
  NODE_END=production gulp

DEVELOPMENT
  to create vendors bundle (once):
    gulp frontend:browserify-vendors

  gulp

NPM
  npm install          // installs locally according to package.json
  npm install gulp -g  // installs globally according to package.json
  npm install 6to5 -g  // --||--
  npm install bower -g // --||--

  npm outdated --depth 0
  npm outdated --depth 0 -g
  npm update <package> --save
  npm update <package> --save -g

  run fixes if needed:
  . bin/fixes/<package>.sh

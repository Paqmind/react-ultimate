import createParseBody from "./middlewares/parse-body";
import createParseParams from "./middlewares/parse-params";
import createParseQuery from "./middlewares/parse-query";

export default {
  createParseBody,
  createParseParams,
  createParseQuery,
}